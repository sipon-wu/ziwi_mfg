services:
  backend:
    build: ./backend
    container_name: cloud-backend
    ports:
      - "127.0.0.1:8090:8000"
    # 密钥统一放在 rsync 树之外的 /opt/cloud-secrets/.env，避免整目录 rsync --delete 误删生产密钥
    env_file:
      - /opt/cloud-secrets/.env
    environment:
      - CLOUD_JWT_ACCESS_EXPIRE_MINUTES=30
      - CLOUD_JWT_REFRESH_EXPIRE_DAYS=7
      - CLOUD_KEY_DIR=/app/keys
      - CLOUD_CORS_ORIGINS=*
      - PLATFORM_ADMIN_EMAIL=admin@ziwi.cn
    volumes:
      - cloud_keys:/app/keys
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: cloud-frontend
    ports:
      - "127.0.0.1:3000:80"
    depends_on:
      - backend
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    container_name: cloud-db
    env_file:
      - /opt/cloud-secrets/.env
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_DB=cloud_idp
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - cloud_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  cloud_keys:
  cloud_pgdata:
